fastapi>=0.100.0
uvicorn>=0.23.0
supabase>=2.0.0
pydantic>=2.0.0
python-dotenv>=1.0.0
jinja2>=3.1.0

# MCP server (mcp_server.py) — optional if you only run the FastAPI app

httpx
jinja2
redis>=5.0.0
